name: Variable

on: 
  workflow_dispatch

env:
  MY_VAR: workflow  

jobs:
  variables:
    env:
      MY_VAR: jobs  
    runs-on: ubuntu-latest
    steps:
      - name: Print
        env:
          MY_VAR: steps  # Step-level override
        run: |
          echo '${{secrets.PASSWORD}}'

